<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CRT" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textViewStyle">@font/proxima</item>

        <item name="android:textColorPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="android:fontFamily">@font/proxima</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyActionBar" parent="Theme.MaterialComponents.DayNight">
        <item name="android:background">#000000</item>
    </style>
    <style name="Theme.CRT.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CRT.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CRT.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>